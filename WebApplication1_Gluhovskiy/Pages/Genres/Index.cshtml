@page
@model WebApplication1_Gluhovskiy.Pages.Genres.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Genre[0].GenreID)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Genre[0].GenreName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Genre[0].GenreDescription)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Genre) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.GenreID)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.GenreName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.GenreDescription)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.GenreID">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.GenreID">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.GenreID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
